[project]
name = "The-AutoGrad-Engine"
version = "1.0.0"
description = "A pure-Python scalar autograd engine built from scratch"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "graphviz>=0.21",
    "matplotlib>=3.11.1",
    "pytest>=9.1.1",
    "rich>=15.0.0",
]
